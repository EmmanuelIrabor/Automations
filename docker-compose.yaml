services:

  n8n:
    image: n8nio/n8n:2.33.5

    ports:
      - "5678:5678"

    environment:
      - N8N_EDITOR_BASE_URL=${N8N_EDITOR_BASE_URL}

    volumes:
      - n8n_data:/home/node/.n8n

volumes:
  n8n_data:
