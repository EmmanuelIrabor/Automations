services:
  n8n:
    image: n8nio/n8n:2.33.5
    container_name: n8n-email-attachment
    restart: unless-stopped

    ports:
      - "4000:5678"

    env_file:
      - .env

    volumes:
      - n8n_data:/home/node/.n8n

volumes:
  n8n_data: